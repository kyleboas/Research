# Research

## Quick start

Use the setup script to bootstrap the project environment:

```bash
bash scripts/setup.sh
```

Then open `.env`, fill in real credentials, and run:

```bash
pytest -q
```

For iPhone-focused setup details, see `docs/setup.md`.
